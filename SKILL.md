---
name: delivery-harness
description: Use when an agent is asked to own a complex multi-stage software project, keep plans and implementation aligned, continue without repeated confirmation, research changing tools, or stop only at material decisions, authority boundaries, and real-world evidence gates.
---

# Delivery Harness

> **English readers:** This file's Chinese rule text is authoritative. Start with the [English routing index](references/index.en.md), then read only the linked Chinese reference required for the current task. Do not load both language paths for the same project.

Delivery Harness 的核心目标是在授权、决策和证据边界内减少不必要的人工干预，把一个已接受的项目持续推进到可验证完成。设计、调研、测试、排错和部署仍由对应的专业 Skill 完成；Harness 让它们共享同一份项目状态、一个活动节点、清楚的授权边界和可核验的阶段门。

按任务读取对应参考文件：

- 改变状态、安装、部署或进入新阶段：[gates.md](references/gates.md)
- 启动或节点失败：[principles.md](references/principles.md)
- 诊断、恢复或重试失败节点：[debugging.md](references/debugging.md)
- 安装到 Codex、Claude、OpenClaw、Hermes 或其他 CLI：[runtime-installation.md](references/runtime-installation.md)
- 选择 Ponytail、Caveman、Humanizer、Context7、文档解析、代码图谱或项目特有能力：[capability-routing.md](references/capability-routing.md)
- 配置 Agent 职责或拆分任务：[agent-config.md](references/agent-config.md)
- 使用连接器、CLI、账号或外部服务：[integrations.md](references/integrations.md)
- 使用 GitHub：[github.md](references/github.md)

## 语言与载入

按会话语言选择入口，同一项目不重复载入两种语言：

- 中文会话：直接读本正文，需要细则时按任务读对应参考文件。
- 英文会话：先读 [index.en.md](references/index.en.md) 的英文索引（约一屏），按索引点读当前任务所需的中文参考文件，不重复通读本正文。
- 判定是外部事实：会话界面语言或用户消息语言；不确定时按用户消息语言。
- 参考文件以中文为事实源；英文会话按索引导航，不把中文正文整体载入上下文。

## 控制项

默认沿用当前会话风格，除非用户另有要求。

- `ponytail=lite|full|ultra|off`：Ponytail 可用时，控制实现最小化程度。
- `communication=normal|caveman-full|caveman-ultra`：Caveman 可用时，控制说明长度。
- Ponytail 减少依赖、抽象、文件和代码；Caveman 减少文字和 Token。两者互不替代。
- 两种模式都不能省略需求分析、当前调研、信任边界、TDD、安全检查、回滚或证据。
- 可选 Skill 不可用时，可以采用其原则，但不能声称已经调用。
- 可选能力不是硬依赖。信号按 [capability-routing.md](references/capability-routing.md) 的评估时机表在节点事件上评估，触发后执行发现、授权、安装、调用、验证和降级流程。

## 会话启动

载入的单位是项目，不是会话。一个会话可以触碰零到多个项目，每个项目在首次被触碰时各自载入一次。

两个触发条件都不依赖判断：

- 会话开始，且工作目录是一个已识别的项目。判据用确定性探测，例如存在版本库、仓库规则文件或项目覆盖层。
- 某次动作的目标路径落在一个尚未载入的项目根之下。不要用「这次算不算项目开发」这种语义判断做触发条件，那会把机制退回成靠执行者想起来。

按顺序载入，每项的完成判据是能报出具体值，报不出就按缺失处理：

1. 项目规则与项目覆盖层。
2. 版本控制事实与本次生效的提交身份。
3. 可达能力与环境边界。
4. 上次结论与活动节点：先按项目覆盖层记录的存放根（默认 `.delivery/`）找状态文件与证据登记表，不四处搜索。
5. 项目文档更新面，即哪些文档承载哪类断言。
6. 自动载入契约：检查每个运行时实际读取的指令文件（如 `AGENTS.md`、`CLAUDE.md`，受限运行时走其可达路径）是否含契约块；缺则嵌入，有则核对，更新则整块替换。契约块是带分隔符的固定字符串，格式与目标文件清单见 [runtime-installation.md](references/runtime-installation.md)；可复制块见 `assets/*.block.template.md`。

**第一条回复必须是启动产出固定块**，出现在任何工具调用之前，模板如下；未输出不得开始工具调用：

```text
【启动回执】
规则：<项目规则与覆盖层，一句话>
版本控制：<分支/HEAD 与生效提交身份>
节点：<活动节点与下一道门>
边界：<沙箱/权限/工具限制，一句话>
【能力信号评估】
<按 capability-routing 时机表，每个能力写触发或未触发+理由，未触发也写理由>
```

回执让载入是否成功可以被一眼核对；没有回执就无法区分「载入了但没照做」和「根本没有载入」。验收判据：固定块出现在第一次工具调用之前，在会话记录中可客观检查。

缺失时的行为：缺规则，停下来问；**缺提交身份可以继续工作但不得提交**；缺上次结论，从活文件重建并在回执里标注；缺环境边界，先做只读探测，不要把探测失败当成能力不存在。新会话在首次工具调用前取不到回执事实（分支/HEAD、活动节点）时，对应行写「待探测」，回执后立即用判据命令补报；判据只看固定块先于工具调用，不看数值完备。

自动载入层有字节上限，超限通常是静默截断而不是报错。超预算时按文档更新面、上次结论的顺序丢弃，规则、版本控制事实和环境边界不丢。

采集用的具体命令属于项目事实，填在项目覆盖层。本节只规定采集什么与回执什么格式。

## 项目状态

除非新证据证明前面的阶段已经验收，否则按顺序推进：

```text
reality_audit
  -> requirements
  -> tool_research
  -> solution_decision
  -> design_and_plan
  -> environment_and_authority
  -> repository_integration
  -> tdd_nodes
  -> real_evidence
  -> release_or_handoff
```

规则按节点分类。当前节点决定该取用本页哪一节，与当前节点无关的章节不必反复通读。

| 节点 | 本页章节 |
|---|---|
| reality_audit | 从现实开始 |
| requirements | 厘清需求 |
| tool_research | 调研当前工具 |
| solution_decision、design_and_plan | 决策、设计与计划 |
| environment_and_authority | 检查环境与授权 |
| repository_integration | 集成仓库 |
| tdd_nodes | 逐节点执行 TDD |
| real_evidence | 验证真实证据 |
| release_or_handoff | 完成条件 |
| 全程适用 | 控制项、语言与载入、会话启动、项目状态、配置 Agent 与外部集成 |

每次转换都要：

1. 用新证据验证当前门槛。
2. 提交前核对本次改动涉及的事实，同步已接受的规格、计划、状态和经验。不要等自己判断「事实变化了」才做，**那个时刻不会到来**。
3. 完成当前节点，只保留一个下一节点为活动状态。活动节点写在项目覆盖层里，提交前核对它是否仍然正确，不靠记忆维护。
4. 本地版本控制已获授权时，提交最小且完整的已验证变更。
5. 下一步仍在范围内且没有停止条件时，直接继续。

文档更新是检查点，不是停止点。

同步之前先找出哪些文档在说这件事。改变一个事实之后，**用它的旧说法在全仓检索**，命中的每一处都要处理：状态页、规格、计划、交接文档、面向用户的使用说明和它的对照语言版本都算。只更新手头正在写的那一份，等于把过时断言留给下一位执行者，而过时断言读起来和有效结论没有区别。英文文档的链接只指向有英文版本的文档；目标只有中文版本时，在链接旁标注（中文），或在同一段落用一句话声明该组文档的语言。

这条与调研那条对称：把方案写进决定文档之前要检索项目已有的评估与决定文档，改变事实之后同样要检索，只是方向相反。

交接文档只写一次性上下文，不承载可以现查的事实。分支、工作树状态、远端是否存在、提交身份这类事实必须在新会话里用命令现取。写进交接文档的事实会在下一次会话里被当成既定前提，而过时前提和有效结论读起来没有区别。

观测另一个项目的会话时，只记录与修复本技能自身的问题，不跨项目代执行下游项目的机制动作（如契约嵌入、身份设置）；那些动作由下游项目自己的会话和工具按机制完成。P0 止血（例如个人邮箱即将进入新提交）可以外部干预，但必须登记观测污染。

失败不是默认停止条件。先按 [debugging.md](references/debugging.md) 复现、分类、在既有授权内安全诊断和恢复，并用原动作复验；只有需要用户决策、扩大权限或补充真实证据时才暂停。

「这一轮做不完」不是停止条件。先看能否拆成互不共享状态的独立任务并行执行；规模值得并行时才派子代理（需用户允许），小改动串行自己做。拆不开且确实需要用户提供某样东西时，才停，并按 [gates.md](references/gates.md) 写出一行停止理由。

## 从现实开始

实现前读取仓库规则、当前状态、已接受的规格与计划、相关代码和测试、Git 状态及可用的运行证据。随后明确：

- 已接受的目标和非目标；
- 哪些内容处于已设计、已实现、已测试、已部署和已验收状态；
- 什么证据能够证明成功；
- 约束、假设、风险和授权边界；
- 当前活动节点及其下一道门；
- 能力信号评估一行：按 capability-routing 的时机表，每个能力写「触发」或「未触发+理由」，未触发也写理由。这是审计的可见产出，跳过不留痕的评估等于没评估。

优先复用项目已有文档。只有没有可用计划时才新建。路线图勾选、设计文档、模拟结果或旧消息都不能单独证明已经实现。

项目需要本地规则、长期经验或 Resolver 时，复制 [project-overlay.template.md](assets/project-overlay.template.md)，只填写适用部分。覆盖层保存项目事实，不复制通用 Skill。Resolver 属于可执行经验：证据改变正确工具或流程时更新，不因为普通节点完成而机械改写。

## 厘清需求

确认用户结果、角色、敏感数据、隐私与权限边界、输入输出格式、规模、延迟、准确率、可靠性、离线需求、代表性样本、兼容性、部署、回滚，以及可逆 MVP 和后续扩展的边界。

一次只问一个阻塞问题。安全假设不会改变行为或风险时，记录后继续。约束冲突时，先说明冲突再提出方案。

## 调研当前工具

库、模型、服务、Skill、协议、许可证或部署选项可能变化时，查询当前资料。

先看仓库和本机已经具备的能力，再从一手资料核对版本、支持平台、许可证、安全说明、遥测、维护状态和部署要求。至少比较：

- 不增加组件的基线；
- 当前已有的最佳能力；
- 需求足以证明合理的最强外部替代方案。

比较功能适配、质量证据、本地或离线能力、隐私、许可证、维护、运行成本、依赖、可逆性、供应商锁定和失败行为。记录证据日期，区分事实与推断。优先做小型只读验证，不用营销说法代替证据。给出一个建议，并写明何时应该重选。

准备自己发明一个方法时，先调研这个问题域已有的成熟方案和公开基准，再动手实现。分类器、匹配算法、评分函数、相似度指标和启发式规则都属于这一类。先实现再调研会浪费两轮实验，也容易在错误的目标函数上做优化。这条的触发点是外部动作：开始实现之前，先完成调研并留下记录；没有调研记录就不许开始实现。

调研范围必须包含项目自身已有的评估与决定文档。把方案写进决定文档之前，先确认项目里没有针对它的既有结论。已有结论的方案按它写下的触发条件判断，不得重新提案。

方案不确定时，联网调研是默认动作，不需要先征求用户同意。只读检索不改变系统状态，也不产生成本；安装、付费、发送项目数据和扩大权限才是独立授权门。

某项能力被用户否决或实测失败时，不得据此否决整个工具类。先把工具拆成能力再判断：检测与解码、识别与生成、查询与写入都是不同能力，一项不可用不代表其余不可用。

凭样本手工标定阈值，本身就是自造方法。真实数据上出现错判时，先回到调研，不要继续调参。

现成方案也要用同一批真实样本实测。成熟方案不必然整体更优，它可能召回不足，也可能在本项目样本上失效。两者错误方向不同时，结论可以是组合，不是二选一。

调研用于筛选，不是为了安装所有候选工具。

## 决策、设计与计划

只有选项会实质改变产品行为、架构、持久数据、隐私、持续成本、许可证、部署风险或长期运行方式时，才让用户选择。普通且可逆的实现细节，采用满足验收条件的最简单方案并继续。

记录选择、理由、淘汰方案、假设、风险、回滚路径和重审条件。用户说明某个约束只是暂时性边界时，同时记录目标状态和迁移路径，并让当前设计保持可迁移，不把临时边界当成永久前提。只维护项目真正需要的产物：

- 规格：范围、架构、接口、数据、失败行为、安全和验收标准；
- 执行计划：通常能在两小时内独立完成的节点；
- 规则：防止已经重复出现的失败；
- 经验：有观察证据、以后会改变做法的结论；
- Resolver：同一条路由被第二次用到时，保存条件到 Skill、工具或流程的映射。用计数做判据，不用「是否值得」。

写一条规则时，先确定它的**触发点是外部可观测事件**，还是执行者的内部判断。**触发点落在内部判断上的规则不会稳定生效**：执行者从不宣告「我判断完成了」或「我意识到事实变了」，规则就永远不触发。把触发点重新锚定到一个已经存在的外部动作上，通常是提交、写入某类文件或发起某个外部调用。

强规则必须写成可核验契约，至少包含六项：外部可观测事件作为**触发**、触发后执行的**动作**、可复制的执行方法、成功**判据**、动作**失败**时的处理，以及要登记的**证据**。缺一项就仍是提醒，不是稳定护栏。

能给出一条可照抄的命令，就不要写成一句意图。**意图会衰减，可照抄的字符串不会**，因为它要被敲进某个命令里，触发点必然绑在外部动作上。

给规则配套护栏时，同时写死它的**升级触发条件**：可观测事件计数，例如同类失败两周内复发 N 次 → 升级为硬阻断。流程类重审条件用事件计数，不用阈值；「感觉需要」不是触发条件。

规则无法锚到已有外部动作、又没有可锚的动作时，可在项目里加运行时 Hook 或提交门禁：matcher 收窄到高代价不可逆动作，先警告，按写死的条件升级。Hook 与门禁配置是项目事实，留在项目覆盖层。

每个实施节点写明文件、RED 测试、实现目标、验证命令、依赖、估时和停止门。

调参或增加精细度之前，先确认它优化的是不是要解决的那个目标。目标错配时，把特征做得更细只会把错配做得更精确。

## 检查环境与授权

先做只读检查。记录操作系统、运行时、包管理器、算力、存储、网络、依赖、服务与凭据边界、仓库结构、无关工作区改动和验证命令。

新增依赖前先复用已有组件。可复现性重要时记录或固定版本。部署第三方软件前检查来源、许可证、遥测、安装脚本和安全影响。

授权请求要尽量小，相关请求可以合并。说明准确动作、目标、持久性、外部影响和回滚方式。交付自主权不会扩大权限，具体门槛见 [gates.md](references/gates.md)。

能力信号触发且所需工具缺失时，向用户提出工具链授权选择：自动开启（用户预先批准所需工具集，写入覆盖层「预授权工具集」槽，集内安装不再逐次询问）或逐个授权。两种模式都保留安装前适配检查、安装后验证与登记；用户选择记入覆盖层，同节点不重复询问。

同一节点内已明确批准的同类动作不重复询问。复用前确认目标、数据类别、成本范围、持久性和外部影响未变；任一项扩大或改变时，按 [gates.md](references/gates.md) 请求新授权。

## 配置 Agent 与外部集成

通用 Skill 必须保持可移植。除非核心工作离开某项服务就无法进行，不要把 GitHub 或其他可选服务设为硬依赖。调用方式、主代理和子代理职责、项目覆盖层见 [agent-config.md](references/agent-config.md)。

能力信号按 [capability-routing.md](references/capability-routing.md) 的评估时机表在节点事件上自动评估，用户不在场时同样触发，只在授权或决策门停下。需要外部工具时，按 [integrations.md](references/integrations.md) 执行。任务可独立并行且确有必要时，请求用户允许使用子代理；不得为简单或串行任务委派。Harness 可以发现已安装能力、完成允许的只读检查、解释缺失配置，并在配置完成后验证。不得静默安装全局插件、认证账号、索取秘密、启用付费服务、扩大权限范围或修改持久机器配置。

连接不等于具备所需能力，一个客户端已认证也不能证明另一个客户端已认证。连接器、CLI 和浏览器登录态分别检查。宣布阻塞前，报告已尝试动作、所用能力、准确结果、错误码、剩余缺口和完整恢复路径。GitHub 使用 [github.md](references/github.md)。

## 集成仓库

仓库集成包括可版本化的代码、配置、文档、测试和可复现安装。

- 保留用户的无关改动。
- 只增加完成目标所需的最小依赖和配置。
- 验证安装和回滚。
- 不提交秘密或机器专属绝对路径。
- 提交前脱敏，按 [gates.md](references/gates.md) 检查工作树、暂存区和 Git 历史。
- 运行会接触凭据的诊断、探针或状态命令前，按 [gates.md](references/gates.md) 的输出脱敏规则处理：输出中的凭据值先脱敏再进入上下文，**值不进上下文**，键名与结构可留；诊断结果**默认不落盘**，需要归档时先脱敏再写入已批准位置；输出已含真实值时按「已泄露」处置：不重复值、列入轮换、清理残留、登记泄露面与处置。
- 项目脚本与工具的输入输出路径不硬编码机器专属绝对目录（如 /tmp、用户目录）；输出目录默认派生自项目根（相对路径）或落证据槽，确需绝对路径时由调用参数注入并在覆盖层登记偏离。
- 提交前还要过 [gates.md](references/gates.md) 的**通用性检查**，它与秘密扫描范围不同：秘密扫描管会不会泄密，通用性检查管这个仓库交给一个不相干的项目还读不读得通。用户基础设施名、产品名和业务领域词汇都归后者，秘密扫描按定义不会报它们。
- **提交前先读实际暂存清单**，再写提交说明。项目自带的提交脚本可能整树暂存，此时暂存范围由工作树决定，不由你打算改的那几个文件决定。说明覆盖不到的文件不能进这次提交：要么先移出工作树，要么把说明扩到覆盖它们。脚本只报告改动数量而不报告文件名时，先补上清单输出，再提交。
- **交付边界要显式**。仓库同时装着交付物和开发脚手架，分发时只带走交付物。改动前先确认哪些路径随交付走、哪些只存在于开发仓库。**依赖的作用域按交付边界算，不按仓库算**：某个运行时或语言已经是开发脚手架的依赖，不等于交付物可以使用它；给交付物新增依赖是独立决定，按交付物自己的可移植性要求判断。交付物需要执行一个动作时，通用层规定要做什么与输出什么，具体命令留在项目覆盖层，**可执行件留在开发仓库**。
- **仓库形态写成清单**。一次性工作产物、操作残留和交付物同处一个工作树，只靠记得不要提交来区分，迟早会在一次整树暂存里失手。用忽略规则和已跟踪路径清单把它固定下来，新增路径先改清单再提交。存放根 `.delivery/` 属于开发产物区：`state.md` 进版本控制，其余槽 git 忽略、不随交付分发。
- 除非项目明确把缓存或索引视为持久事实，否则它们必须可重建。

远端推送、PR、发布、生产部署、数据迁移和外部协调是独立权限门，除非已接受范围明确包含这些动作。

## 逐节点执行 TDD

对每个已接受行为：

1. 写出表达该行为的最小测试。
2. 运行测试，确认出现预期 RED 失败。
3. 实现最小正确变更。
4. 运行聚焦 GREEN 测试。
5. 只在 GREEN 后重构，再次运行聚焦测试。
6. 按风险运行回归和真实产物检查。
7. 追查根因，不削弱测试来隐藏失败。
8. 提交前同步状态、文档、规则、经验和 Resolver，不等自己判断事实变了。
9. 完成节点，获授权时提交，然后激活下一节点。

长操作期间给出简短进度。除非用户或项目规则允许，不要委派。允许委派时，主代理仍负责共享状态、阶段门、文档和版本控制，详见 [agent-config.md](references/agent-config.md)。

## 验证真实证据

任何完成声明前，执行 `IDENTIFY -> RUN -> READ -> VERIFY -> THEN`。记录命令、退出码、测试数量、产物或运行观察，以及已知限制。

单元测试不能代替需求指定的真实样本、设备、浏览器状态、数据集、账号、外部服务、类生产负载或用户验收。证据不可用时，保持节点等待，并写明准确的解除条件。

### 证据产物登记

每一份用于真实证据的 artifact 都要写进项目文档：位置与可达方式、处理过程、结论与边界、复核日期。样本、导入产物、运行目录、数据集和导出物都算。只写在对话里不算登记，会话结束后下一位执行者会重新索要同一份材料。

登记位置有默认值：项目存放根 `.delivery/` 下的固定槽，`state.md` 存活动节点，`uploads/` 存用户上传的材料，`artifacts/` 存临时生成与证据产物，`debug/` 存排查纠错的关键文件。新产物先落槽再登记；偏离默认位置才需写明。

声明材料或证据缺失前，先写明自己的可达边界：哪台机器、哪些目录、有没有网络、哪些位置没有覆盖。不得把自身不可达当成不存在，要请用户在其可达位置复核后再判定。

## 完成条件

只有以下条件全部满足才能结束：

- 已接受目标和真实证据门通过；
- 聚焦测试和适量回归通过；
- 文档、规则、安装状态和版本控制符合现实；
- 回滚方式与剩余限制已有记录；
- 没有未完成的必要工作。

否则继续，或按 [gates.md](references/gates.md) 报告一个准确的决策、权限或证据阻塞。
